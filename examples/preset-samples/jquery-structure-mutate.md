# jQuery Demo 3: Structure Mutations

目标：演示 `append/prepend/before/after/replaceWith/remove/empty`。

## Source Blocks

Paragraph A

Paragraph B

- list item one
- list item two
  - nested child one
  - nested child two

1. ordered one
2. ordered two

### Code Area

```ts
const foo = 1;
const bar = 2;
console.log(foo + bar);
```
