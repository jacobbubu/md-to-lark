# jQuery Demo 1: Basic Select + Replace

这份文档用于演示以下 API：

- `$('text,bullet').contains('foo').replaceText(/foo/g, 'bar')`
- `$('heading1').text((_, old) => old + ' (checked)')`

foo in paragraph line 1.

This paragraph also contains foo and a [demo link](https://example.com/path).

- bullet foo item A
- bullet item B
- bullet foo item C

## Inline Styles Sample

Here is **bold**, _italic_, ~~strikethrough~~, and `inline code`.
